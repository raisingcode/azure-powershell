name: manage-azure-policy-0ebd78ea
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'policies/Deploy_Dependency_agent_for_Windows_virtual_machine_scale_sets_3be22e3b-d919-47aa-805e-8985dbeb0ad9/**'
#      - .github/workflows/manage-azure-policy-0ebd78ea.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_0ebd78ea}}
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          policies/Deploy_Dependency_agent_for_Windows_virtual_machine_scale_sets_3be22e3b-d919-47aa-805e-8985dbeb0ad9/**
